import { ReactElement } from "react";

/**
 * Loader Function
 * @returns the main content of the Loader page
 */
const Loader = (): ReactElement => {
  return (
    <main>
      <h1>Loading...</h1>
    </main>
  );
};

export default Loader;
